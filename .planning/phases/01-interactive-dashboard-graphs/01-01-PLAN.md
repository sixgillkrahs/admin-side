---
phase: 01-interactive-dashboard-graphs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n.ts
  - src/features/dashboard/components/DashboardOverview.tsx
autonomous: true
requirements:
  - DASH-02
user_setup: []

must_haves:
  truths:
    - "D-01: src/features/dashboard/components/DashboardOverview.tsx imports components from Recharts"
    - "D-02: src/features/dashboard/components/DashboardOverview.tsx displays LineChart and BarChart"
    - "D-03: src/features/dashboard/components/DashboardOverview.tsx renders charts below KPI cards"
    - "D-04: src/features/dashboard/components/DashboardOverview.tsx charts respond to language changes dynamically"
    - "D-05: src/features/dashboard/components/DashboardOverview.tsx charts use CSS variables for theme borders and colors"
  artifacts:
    - "src/features/dashboard/components/DashboardOverview.tsx"
  key_links: []
---

<objective>
Install Recharts library, add translation dictionaries for chart details, and implement interactive responsive LineChart and BarChart widgets in the dashboard overview screen.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-interactive-dashboard-graphs/01-CONTEXT.md
@.planning/phases/01-interactive-dashboard-graphs/01-UI-SPEC.md
@.planning/phases/01-interactive-dashboard-graphs/01-RESEARCH.md
@.planning/phases/01-interactive-dashboard-graphs/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Chart Localization Dictionaries</name>
  <files>
    src/lib/i18n.ts
  </files>
  <read_first>
    .planning/phases/01-interactive-dashboard-graphs/01-UI-SPEC.md
  </read_first>
  <action>
    Modify `src/lib/i18n.ts` to add translation keys under the `dashboard` namespace:
    - `dashboard.charts.trafficTitle` / `dashboard.charts.trafficSub`: "API Request Volume" / "Số yêu cầu trong 7 ngày qua"
    - `dashboard.charts.sessionsTitle` / `dashboard.charts.sessionsSub`: "Hourly Sessions" / "Số phiên được ghi nhận theo giờ"
    - `dashboard.charts.requests`: "Requests" / "Yêu cầu"
    - `dashboard.charts.sessions`: "Sessions" / "Phiên"
    - Day names: "Mon", "Tue", etc. / "T2", "T3", etc.
  </action>
  <verify>
    Ensure compiling passes successfully:
    `npm run build`
  </verify>
  <done>Chart translation dictionaries are registered in English and Vietnamese.</done>
</task>

<task type="auto">
  <name>Task 2: Build Line and Bar Graphs in DashboardOverview</name>
  <files>
    src/features/dashboard/components/DashboardOverview.tsx
  </files>
  <read_first>
    .planning/phases/01-interactive-dashboard-graphs/01-RESEARCH.md,
    src/lib/i18n.ts
  </read_first>
  <action>
    1. Install `recharts` package dependency (D-01):
       `npm install recharts`
       
    2. Modify `src/features/dashboard/components/DashboardOverview.tsx` (D-03):
       - Import Recharts components: `ResponsiveContainer`, `LineChart`, `Line`, `BarChart`, `Bar`, `XAxis`, `YAxis`, `CartesianGrid`, `Tooltip`, `Legend`.
       - Declare mock datasets for LineChart (daily request volume over 7 days) and BarChart (sessions per hour of the day).
       - Translate chart labels, legends, and XAxis day names dynamically depending on active locale (D-04).
       - Render a 2-column grid layout under the KPI cards displaying the LineChart and BarChart side-by-side (D-02).
       - Style tooltips, borders, lines, and chart lines using CSS theme variables (D-05) to ensure visual coherence.
  </action>
  <verify>
    Ensure build compiles and ESLint checks pass:
    `npm run lint` and `npm run build`
  </verify>
  <acceptance_criteria>
    - `DashboardOverview.tsx` imports Recharts components.
    - Two charts (Line and Bar) are rendered on the screen below the KPI cards.
    - Build completes without warnings.
  </acceptance_criteria>
  <done>Interactive Line and Bar graphs are rendered in DashboardOverview with full translation capability.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build compiles with `npm run build`
- [ ] Lint checks pass with `npm run lint`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-interactive-dashboard-graphs/01-01-SUMMARY.md`
</output>
