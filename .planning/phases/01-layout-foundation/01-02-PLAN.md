---
phase: 01-layout-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/common/Sidebar.tsx
  - src/components/common/Header.tsx
  - src/components/common/Layout.tsx
  - src/App.tsx
autonomous: true
requirements:
  - STRUCT-02
user_setup: []

must_haves:
  truths:
    - "src/components/common/Sidebar.tsx renders Dashboard and RBAC navigation buttons"
    - "src/components/common/Header.tsx renders top header panel"
    - "src/components/common/Layout.tsx wraps layout shell"
    - "src/App.tsx renders Layout component"
  artifacts:
    - "src/components/common/Sidebar.tsx"
    - "src/components/common/Header.tsx"
    - "src/components/common/Layout.tsx"
    - "src/App.tsx"
  key_links:
    - "src/App.tsx -> src/components/common/Layout.tsx"
    - "src/components/common/Layout.tsx -> src/components/common/Sidebar.tsx"
    - "src/components/common/Layout.tsx -> src/components/common/Header.tsx"
---

<objective>
Build the common Sidebar, Header, and Layout components to provide a responsive admin shell, and integrate it into App.tsx to enable active page tab toggling.

Purpose: Delivers requirement STRUCT-02 (D-03 and D-04) establishing the visual foundation and user interactions for the administrative control panel.
Output: Sidebar navigation, top header page details, and application layout wrapper.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-layout-foundation/01-CONTEXT.md
@.planning/phases/01-layout-foundation/01-RESEARCH.md
@.planning/phases/01-layout-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Sidebar, Header, and Layout Shell Components</name>
  <files>
    src/components/common/Sidebar.tsx,
    src/components/common/Header.tsx,
    src/components/common/Layout.tsx
  </files>
  <read_first>
    .planning/phases/01-layout-foundation/01-RESEARCH.md,
    src/components/ui/button.tsx
  </read_first>
  <action>
    Create the shell UI elements in `src/components/common/`:
    
    1. `Sidebar.tsx`:
       - Renders a sidebar container with absolute positioning on mobile (collapsible) and fixed width (`w-64`) on medium+ screens.
       - Displays premium typography, logo section, and navigation links ("Dashboard Overview", "RBAC Access").
       - Highlights the active tab with visual cues (border/background changes matching shadcn rules).
       - Uses lucide-react icons for navigation visual quality.
       
    2. `Header.tsx`:
       - Renders top application bar matching dark/light themes.
       - Displays page title aligned with the active tab.
       - Includes responsive mobile navigation toggle button.
       
    3. `Layout.tsx`:
       - Wraps Sidebar, Header, and page content under a grid/flex container.
       - Leverages activeTab state to propagate page selections down.
  </action>
  <verify>
    Verify files are successfully created and compile:
    `npm run build`
  </verify>
  <acceptance_criteria>
    - `src/components/common/Sidebar.tsx` imports `lucide-react`
    - `src/components/common/Layout.tsx` renders `<Sidebar` and `<Header`
  </acceptance_criteria>
  <done>Sidebar, Header, and Layout components are written and compiled.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Shell Layout into App.tsx Root component</name>
  <files>
    src/App.tsx
  </files>
  <read_first>
    src/App.tsx,
    src/components/common/Layout.tsx
  </read_first>
  <action>
    Replace the default template code in `src/App.tsx`:
    - Implement `activeTab` state ("dashboard" | "rbac") using `useState`.
    - Import and render `Layout` component, wrapping the corresponding feature placeholders (`DashboardOverview` or `RbacManagement`) conditional on the `activeTab` value.
    - Remove the unused Vite/React boilerplate SVGs and App.css rules that are redundant.
  </action>
  <verify>
    Ensure lint and build pass without error:
    `npm run lint` and `npm run build`
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` imports `Layout` from `@/components/common/Layout`
    - `src/App.tsx` imports `DashboardOverview` and `RbacManagement`
    - Boilerplate counter button and SVGs are removed
  </acceptance_criteria>
  <done>App.tsx wraps views with the Layout shell, wired to dynamic feature components.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Ensure `npm run build` passes with zero errors
- [ ] Run `npm run lint` with zero errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-foundation/01-02-SUMMARY.md`
</output>
