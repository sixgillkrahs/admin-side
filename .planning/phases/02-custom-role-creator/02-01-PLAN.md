---
phase: 02-custom-role-creator
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/rbac/types/index.ts
  - src/lib/i18n.ts
  - src/features/rbac/components/RbacManagement.tsx
  - src/features/rbac/components/PermissionMatrix.tsx
  - src/features/rbac/components/EditRoleModal.tsx
files_created:
  - src/features/rbac/components/CreateRoleModal.tsx
autonomous: true
requirements:
  - RBAC-04
user_setup: []

must_haves:
  truths:
    - "D-01: src/features/rbac/components/CreateRoleModal.tsx uses Shadcn UI Dialog and Checkbox primitives"
    - "D-02: src/features/rbac/components/RbacManagement.tsx holds dynamic roles and permissions state"
    - "D-03: CreateRoleModal form validates empty role names and duplicate entries case-insensitively"
    - "D-04: Custom Role Creator form elements, labels, buttons, and errors support dynamic translation switching"
    - "D-05: Renders the custom role in the PermissionMatrix grid and lists it inside EditRoleModal dynamically"
  artifacts:
    - "src/features/rbac/components/CreateRoleModal.tsx"
    - "src/features/rbac/components/RbacManagement.tsx"
  key_links: []
---

<objective>
Implement a Custom Role Creator form modal to define custom system roles with custom permission mappings, integrating them reactively with the permission matrix table and user role editing modal.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-custom-role-creator/02-CONTEXT.md
@.planning/phases/02-custom-role-creator/02-UI-SPEC.md
@.planning/phases/02-custom-role-creator/02-RESEARCH.md
@.planning/phases/02-custom-role-creator/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update i18n Translation Keys & Types</name>
  <files>
    src/lib/i18n.ts,
    src/features/rbac/types/index.ts
  </files>
  <read_first>
    .planning/phases/02-custom-role-creator/02-UI-SPEC.md
  </read_first>
  <action>
    1. Modify `src/features/rbac/types/index.ts` to allow `Role` to be `string` instead of a hardcoded union, or extend the union to support custom role strings.
    2. Add translation resources under `rbac` namespace inside `src/lib/i18n.ts` for English and Vietnamese:
       - `rbac.createRoleBtn`: "Create Custom Role" / "Tạo vai trò tùy chỉnh"
       - `rbac.createRoleModal.title`, `description`, `nameLabel`, `namePlaceholder`, `permissionsLabel`, `errorDuplicate`, `errorEmpty`, `cancel`, `submit`.
  </action>
  <verify>
    Ensure compiling is correct:
    `npm run build`
  </verify>
  <done>Translations and types are updated successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Lift Roles and Permissions State in RbacManagement</name>
  <files>
    src/features/rbac/components/RbacManagement.tsx
  </files>
  <read_first>
    .planning/phases/02-custom-role-creator/02-RESEARCH.md
  </read_first>
  <action>
    1. Lift the roles list (`['Admin', 'Moderator', 'User']`) and their permission mappings into reactive component state inside `src/features/rbac/components/RbacManagement.tsx`.
    2. Pass `roles` list and `rolePermissions` mapping as props down to `PermissionMatrix` and `EditRoleModal` components.
    3. Add a "Create Custom Role" button to the header of the RBAC Management screen.
  </action>
  <verify>
    Check if code compiles without issues:
    `npm run lint`
  </verify>
  <done>State has been successfully lifted and props passed down.</done>
</task>

<task type="auto">
  <name>Task 3: Build CreateRoleModal Component</name>
  <files>
    src/features/rbac/components/CreateRoleModal.tsx
  </files>
  <read_first>
    .planning/phases/02-custom-role-creator/02-UI-SPEC.md
  </read_first>
  <action>
    1. Create a new file `src/features/rbac/components/CreateRoleModal.tsx`.
    2. Use Shadcn UI `Dialog` and `Checkbox` components.
    3. Implement form with:
       - Input field for Role Name.
       - A vertical list of checkboxes for the 4 standard permissions (`read:messages`, `write:messages`, `manage:roles`, `admin:all`) with description labels.
    4. Implement validation check on submit:
       - Error if name is empty.
       - Error if name duplicates an existing role (case-insensitive check).
    5. On successful submit, trigger `onSave` to append role to state and close modal.
  </action>
  <verify>
    Ensure lint and build compilation succeeds:
    `npm run lint` and `npm run build`
  </verify>
  <done>CreateRoleModal is fully implemented with validation and translations.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate CreateRoleModal and Refactor Child Components</name>
  <files>
    src/features/rbac/components/RbacManagement.tsx,
    src/features/rbac/components/PermissionMatrix.tsx,
    src/features/rbac/components/EditRoleModal.tsx
  </files>
  <read_first>
    src/features/rbac/components/CreateRoleModal.tsx
  </read_first>
  <action>
    1. Wire the `CreateRoleModal` into `RbacManagement.tsx` reactively.
    2. Refactor `PermissionMatrix.tsx` to render dynamically based on `roles` and `rolePermissions` passed as props, rather than static arrays.
    3. Refactor `EditRoleModal.tsx` to dynamically populate dropdown choices based on `roles` passed as props.
  </action>
  <verify>
    Run compilation and verification checks:
    `npm run lint` and `npm run build`
  </verify>
  <done>Full dynamic integration of Custom Role Creator is complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build compiles with `npm run build`
- [ ] Lint checks pass with `npm run lint`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-custom-role-creator/02-01-SUMMARY.md`
</output>
