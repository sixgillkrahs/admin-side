---
phase: 02-user-directory-rbac-integration
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/features/rbac/components/EditRoleModal.tsx
  - src/features/rbac/components/RbacManagement.tsx
  - src/features/rbac/index.ts
autonomous: true
requirements:
  - RBAC-03
user_setup: []

must_haves:
  truths:
    - "src/features/rbac/components/EditRoleModal.tsx wraps Radix Dialog Content"
    - "src/features/rbac/components/RbacManagement.tsx mounts EditRoleModal"
    - "src/features/rbac/index.ts exports RbacManagement"
  artifacts:
    - "src/features/rbac/components/EditRoleModal.tsx"
    - "src/features/rbac/index.ts"
  key_links:
    - "src/features/rbac/components/RbacManagement.tsx -> src/features/rbac/components/EditRoleModal.tsx"
---

<objective>
Build the Role Assignment Dialog Modal component using Radix UI primitives, wire it to the user directory table actions, and export RbacManagement from the feature entrypoint index.ts file.

Purpose: Delivers RBAC-03 (D-04) allowing administrators to modify users' system access rights.
Output: EditRoleModal component, coordinator state integration, and public index.ts export.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-directory-rbac-integration/02-CONTEXT.md
@.planning/phases/02-user-directory-rbac-integration/02-UI-SPEC.md
@.planning/phases/02-user-directory-rbac-integration/02-RESEARCH.md
@.planning/phases/02-user-directory-rbac-integration/02-PATTERNS.md
@.planning/phases/02-user-directory-rbac-integration/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EditRoleModal Component and Wire it to Parent State</name>
  <files>
    src/features/rbac/components/EditRoleModal.tsx,
    src/features/rbac/components/RbacManagement.tsx
  </files>
  <read_first>
    .planning/phases/02-user-directory-rbac-integration/02-RESEARCH.md,
    src/features/rbac/components/UserDirectory.tsx
  </read_first>
  <action>
    1. Implement `src/features/rbac/components/EditRoleModal.tsx` using `@radix-ui/react-dialog` primitives:
       - Displays user's name and email as metadata details.
       - Renders a dropdown selector or radio list to select the new Role ('Admin' | 'Moderator' | 'User').
       - Clicking "Save Changes" triggers `onSave(userId, newRole)` callback and closes the modal.
       - Integrates background backdrop overlay and close transitions.
       
    2. Update `src/features/rbac/components/RbacManagement.tsx` to handle modal state:
       - Add state for `selectedUser` (User | null) and `isModalOpen` (boolean).
       - Add callback `handleSaveRole(userId, newRole)` which updates the target user's role badge in the immutable users state list.
       - Mount `<EditRoleModal />` and wire its props to the callback and states.
  </action>
  <verify>
    Ensure compiling passes successfully:
    `npm run build`
  </verify>
  <acceptance_criteria>
    - `src/features/rbac/components/EditRoleModal.tsx` imports `@radix-ui/react-dialog`
    - `src/features/rbac/components/RbacManagement.tsx` updates users state list immutably on save
  </acceptance_criteria>
  <done>EditRoleModal is built, wired to actions, and updates users state immutably.</done>
</task>

<task type="auto">
  <name>Task 2: Export RbacManagement Component from Feature Entrypoint</name>
  <files>
    src/features/rbac/index.ts
  </files>
  <read_first>
    src/features/rbac/index.ts
  </read_first>
  <action>
    Overwrite `src/features/rbac/index.ts` to export `RbacManagement`:
    `export { RbacManagement } from './components/RbacManagement';`
  </action>
  <verify>
    Ensure build and lint succeed:
    `npm run lint` and `npm run build`
  </verify>
  <acceptance_criteria>
    - `src/features/rbac/index.ts` contains `export { RbacManagement }`
  </acceptance_criteria>
  <done>RbacManagement is exported from the modular entry point.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build compiles with `npm run build`
- [ ] Lint checks pass with `npm run lint`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-directory-rbac-integration/02-03-SUMMARY.md`
</output>
