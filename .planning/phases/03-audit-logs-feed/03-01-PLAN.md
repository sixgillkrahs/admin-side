---
phase: 03-audit-logs-feed
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n.ts
  - src/features/rbac/components/RbacManagement.tsx
  - src/features/rbac/components/EditRoleModal.tsx
  - src/features/rbac/components/CreateRoleModal.tsx
files_created:
  - src/features/rbac/components/AuditLogsFeed.tsx
autonomous: true
requirements:
  - AUDIT-01
user_setup: []

must_haves:
  truths:
    - "D-01: src/features/rbac/components/AuditLogsFeed.tsx is created and renders chronological log events"
    - "D-02: src/features/rbac/components/RbacManagement.tsx implements setInterval timer to simulate dynamic system logs"
    - "D-03: RbacManagement.tsx handles triggers to add custom role creation and user role modification events dynamically"
    - "D-04: Log descriptions support translation switching dynamically with i18n parameters"
    - "D-05: Logs use category icons (User, ShieldAlert, KeyRound) and display timestamps"
  artifacts:
    - "src/features/rbac/components/AuditLogsFeed.tsx"
    - "src/features/rbac/components/RbacManagement.tsx"
  key_links: []
---

<objective>
Implement a real-time Security Audit Logs panel displaying chronological system logs, integrating with a background simulation stream and dynamic action triggers from role modifications.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-audit-logs-feed/03-CONTEXT.md
@.planning/phases/03-audit-logs-feed/03-UI-SPEC.md
@.planning/phases/03-audit-logs-feed/03-RESEARCH.md
@.planning/phases/03-audit-logs-feed/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n Translation Keys</name>
  <files>
    src/lib/i18n.ts
  </files>
  <read_first>
    .planning/phases/03-audit-logs-feed/03-UI-SPEC.md
  </read_first>
  <action>
    Add audit translation resources under `rbac` namespace inside `src/lib/i18n.ts` for English and Vietnamese:
    - `rbac.audit.title`: "Security Audit Logs" / "Nhật ký Kiểm toán Bảo mật"
    - `rbac.audit.description`: "Chronological log of administrative operations and access adjustments." / "Nhật ký theo thời gian của các hoạt động quản trị và điều chỉnh quyền truy cập."
    - `rbac.audit.emptyState`: "No audit logs recorded." / "Chưa ghi nhận nhật ký kiểm toán."
    - Event string templates: `rbac.audit.event.login`, `changeRole`, `createRole`, `viewMatrix`.
  </action>
  <verify>
    Ensure compile builds successfully:
    `npm run build`
  </verify>
  <done>Audit log translations are configured successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Build AuditLogsFeed Component</name>
  <files>
    src/features/rbac/components/AuditLogsFeed.tsx
  </files>
  <read_first>
    .planning/phases/03-audit-logs-feed/03-UI-SPEC.md
  </read_first>
  <action>
    1. Create new file `src/features/rbac/components/AuditLogsFeed.tsx`.
    2. Define interfaces for `AuditEvent` (containing `id`, `type`, `timestamp`, `details` payload).
    3. Render the card layout and a scrollable event feed container.
    4. Render individual event rows with corresponding Lucide icons (`User`, `ShieldAlert`, `KeyRound`) and formatted timestamps.
    5. Translate event texts dynamically using `t()` with detailed parameters (e.g., user name, role name).
  </action>
  <verify>
    Ensure code compiles clean:
    `npm run lint`
  </verify>
  <done>AuditLogsFeed component is successfully created.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor RbacManagement Coordinator</name>
  <files>
    src/features/rbac/components/RbacManagement.tsx
  </files>
  <read_first>
    .planning/phases/03-audit-logs-feed/03-RESEARCH.md
  </read_first>
  <action>
    1. Import `AuditLogsFeed` and define `logs` state in `RbacManagement.tsx` (cap at 20 logs).
    2. Add `addLog(type: string, details: Record<string, string>)` helper.
    3. Set up a `useEffect` interval (e.g. 15 seconds) to dispatch random user login and system access audit logs.
    4. Ensure cleanup `clearInterval()` is properly returned to prevent memory leaks.
    5. Render the `<AuditLogsFeed logs={logs} />` panel beneath the User Directory and Permission Matrix.
  </action>
  <verify>
    Verify compilation and lint checks:
    `npm run lint`
  </verify>
  <done>Coordinator state lifting and live stream simulation are completed.</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Dynamic Action Event Triggers</name>
  <files>
    src/features/rbac/components/RbacManagement.tsx,
    src/features/rbac/components/EditRoleModal.tsx,
    src/features/rbac/components/CreateRoleModal.tsx
  </files>
  <read_first>
    src/features/rbac/components/RbacManagement.tsx
  </read_first>
  <action>
    1. Update the `onSave` callbacks in both `EditRoleModal` and `CreateRoleModal` to trigger `addLog` when actions are saved.
    2. In `EditRoleModal`: add a log for `changeRole` containing the target username and the new role.
    3. In `CreateRoleModal`: add a log for `createRole` containing the custom role name.
  </action>
  <verify>
    Ensure project builds successfully:
    `npm run lint` and `npm run build`
  </verify>
  <done>Dynamic triggered logs are configured and verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build compiles with `npm run build`
- [ ] Lint checks pass with `npm run lint`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-audit-logs-feed/03-01-SUMMARY.md`
</output>
