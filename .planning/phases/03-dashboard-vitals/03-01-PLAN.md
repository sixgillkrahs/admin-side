---
phase: 03-dashboard-vitals
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n.ts
  - src/main.tsx
  - src/components/common/Header.tsx
  - src/components/common/Sidebar.tsx
autonomous: true
requirements:
  - I18N-01
user_setup: []

must_haves:
  truths:
    - "D-04: src/lib/i18n.ts initializes i18next and react-i18next libraries"
    - "D-05: src/lib/i18n.ts defines translation dictionaries for English (en) and Vietnamese (vi)"
    - "D-06: src/components/common/Header.tsx mounts a language switcher toggle button"
  artifacts:
    - "src/lib/i18n.ts"
  key_links:
    - "src/main.tsx -> src/lib/i18n.ts"
---

<objective>
Configure the multi-language support framework using i18next, implement a persistent language toggle switcher in the global Header, and translate common layout text labels.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-vitals/03-CONTEXT.md
@.planning/phases/03-dashboard-vitals/03-UI-SPEC.md
@.planning/phases/03-dashboard-vitals/03-RESEARCH.md
@.planning/phases/03-dashboard-vitals/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create i18n Configuration Module</name>
  <files>
    src/lib/i18n.ts,
    src/main.tsx
  </files>
  <read_first>
    .planning/phases/03-dashboard-vitals/03-CONTEXT.md
  </read_first>
  <action>
    1. Create `src/lib/i18n.ts`:
       - Import `i18n` from `i18next` and `initReactI18next` from `react-i18next`.
       - Define resource dictionaries for `en` (English) and `vi` (Vietnamese).
       - Translate layout strings: sidebar navigation names ("Dashboard", "RBAC Access"), header texts, language names.
       - Initialize `i18n` loading active language from `localStorage` (defaulting to 'en').
    2. Modify `src/main.tsx`:
       - Import `./lib/i18n` at the top of the file to load the initializations.
  </action>
  <verify>
    Ensure compiling passes successfully:
    `npm run build`
  </verify>
  <done>Core i18next dictionaries and initialization module created and wired into entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Language Switcher and Translate Header & Sidebar</name>
  <files>
    src/components/common/Header.tsx,
    src/components/common/Sidebar.tsx
  </files>
  <read_first>
    src/lib/i18n.ts
  </read_first>
  <action>
    1. Modify `src/components/common/Header.tsx`:
       - Use the `useTranslation` hook.
       - Render a premium switcher control (e.g. a round button showcasing "EN" / "VI") that toggles the language via `i18n.changeLanguage()`.
       - Persist selection in `localStorage` inside the toggle handler.
       - Translate page labels.
    2. Modify `src/components/common/Sidebar.tsx`:
       - Use `useTranslation` to translate sidebar menu names ("Dashboard", "RBAC Access" / "Bảng điều khiển", "Quyền truy cập RBAC") and footer accounts info.
  </action>
  <verify>
    Ensure build compiles cleanly:
    `npm run build`
  </verify>
  <done>Language toggle switcher mounts in Header, and layouts translate reactively.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build compiles with `npm run build`
- [ ] Lint checks pass with `npm run lint`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-vitals/03-01-SUMMARY.md`
</output>
