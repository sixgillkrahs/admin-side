---
phase: login-layout-branding-visuals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n.ts
  - src/App.tsx
  - src/features/auth/components/LoginScreen.tsx
  - src/features/auth/index.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []

must_haves:
  truths:
    - "LoginScreen is rendered on startup if the user is not logged in"
    - "Clicking on the EN/VI button in the LoginScreen switcher changes the page language reactively"
  artifacts:
    - "src/features/auth/components/LoginScreen.tsx"
    - "src/features/auth/index.ts"
  key_links:
    - "src/App.tsx imports LoginScreen from src/features/auth"
---

<objective>
Build the responsive split-screen Login page layout structure with a brand graphic panel, interactive credential form layout, and fully integrated i18n support.

Purpose: Set up the authentication portal visual entry point for the admin panel.
Output: LoginScreen component, i18n resource dictionaries, and App routing state binding.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/App.tsx
@src/lib/i18n.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n Translation Resources for Login</name>
  <files>src/lib/i18n.ts</files>
  <read_first>src/lib/i18n.ts</read_first>
  <action>
    Add the translation dictionary under the 'auth' key inside the 'translation' namespaces for both English ('en') and Vietnamese ('vi') in src/lib/i18n.ts.
    Specifically, add translations for:
    - portalTitle (Business Chat Admin Portal / Cổng Quản trị Business Chat)
    - loginTitle (Sign In / Đăng nhập)
    - loginSub (Enter your credentials to access the console / Nhập thông tin để truy cập hệ thống quản trị)
    - emailLabel (Email Address / Địa chỉ Email)
    - emailPlaceholder (name@example.com / name@example.com)
    - passwordLabel (Password / Mật khẩu)
    - passwordPlaceholder (•••••••• / ••••••••)
    - submitButton (Sign In / Đăng nhập)
    - brandQuote (Secure Admin Portal / Cổng quản trị bảo mật)
    - brandSub (Manage workspace users, roles, and real-time audit logs. / Quản lý thành viên, vai trò và lịch sử kiểm toán bảo mật.)
  </action>
  <verify>Check that src/lib/i18n.ts compiles and the resource keys are registered correctly.</verify>
  <acceptance_criteria>
    - src/lib/i18n.ts contains the 'auth' object key with 'portalTitle' in both 'en' and 'vi' sections.
  </acceptance_criteria>
  <done>Resource keys successfully registered in src/lib/i18n.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Build the LoginScreen Component UI</name>
  <files>src/features/auth/components/LoginScreen.tsx, src/features/auth/index.ts</files>
  <read_first>src/components/common/Header.tsx</read_first>
  <action>
    Create the LoginScreen component under src/features/auth/components/LoginScreen.tsx:
    - Implement a split-screen container: 50% left side branding panel (hidden on mobile, shown on md and larger viewports), 50% right side card/form wrapper.
    - Left side: Apply a rich mesh gradient styling (using CSS styling variables, Tailwind v4 bg-gradient-to-br from-indigo-900 via-slate-900 to-purple-950) with glassmorphism overlays, displaying the title ("Business Chat Admin Portal") and description texts.
    - Right side: Card element containing:
      - Language Switcher button in the top right corner. Clicking toggles between English and Vietnamese using the i18next hook.
      - Centered Login Form with credentials fields (Email and Password) and a Sign In button. (Form functionality will be simulated to succeed when clicked).
    Create public exports in src/features/auth/index.ts.
  </action>
  <verify>Check that the component is created and imports standard UI elements cleanly.</verify>
  <acceptance_criteria>
    - src/features/auth/components/LoginScreen.tsx exists.
    - src/features/auth/index.ts exports LoginScreen.
  </acceptance_criteria>
  <done>LoginScreen component UI built and exported.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Login Screen into App Entry Point</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
    Add a reactive state parameter `isLoggedIn` inside App.tsx (defaulting to false).
    - If `isLoggedIn` is false, render `<LoginScreen onLoginSuccess={() => setIsLoggedIn(true)} />`.
    - If `isLoggedIn` is true, render the main `<Layout>` with Dashboard/RBAC screens.
  </action>
  <verify>Run the compiler and check that there are no linter/build errors.</verify>
  <acceptance_criteria>
    - App.tsx renders `<LoginScreen>` when `isLoggedIn` is false.
  </acceptance_criteria>
  <done>LoginScreen integrated into App entry point state flow.</done>
</task>

</tasks>
