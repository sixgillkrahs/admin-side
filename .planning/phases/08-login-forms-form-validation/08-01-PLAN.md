---
phase: login-forms-form-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n.ts
  - src/features/auth/components/LoginScreen.tsx
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
user_setup: []

must_haves:
  truths:
    - "Clicking Sign In with empty or invalid credentials displays validation messages inline"
    - "Clicking Sign In with valid credentials displays a loading spinner and disables all fields for 1.5 seconds"
    - "After 1.5 seconds, the user is redirected to the dashboard console"
  artifacts:
    - "src/features/auth/components/LoginScreen.tsx"
  key_links: []
---

<objective>
Implement credentials form validation, loading spinner states, and redirection timeout handlers inside the LoginScreen component.

Purpose: Provide secure client-side form validation and an intuitive loading redirection experience.
Output: Localized validation messages, form disabling state, spinner button overlay, and setTimeout redirect.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/auth/components/LoginScreen.tsx
@src/lib/i18n.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add i18n Translation Keys for Form Errors and Loading</name>
  <files>src/lib/i18n.ts</files>
  <read_first>src/lib/i18n.ts</read_first>
  <action>
    Add the translation keys under the 'auth' object in src/lib/i18n.ts for English ('en') and Vietnamese ('vi'):
    - errorInvalidEmail (Please enter a valid email address. / Vui lòng nhập địa chỉ email hợp lệ.)
    - errorShortPassword (Password must be at least 6 characters. / Mật khẩu phải có ít nhất 6 ký tự.)
    - loadingText (Signing in... / Đang đăng nhập...)
  </action>
  <verify>Check that src/lib/i18n.ts compiles and contains the new error message keys.</verify>
  <acceptance_criteria>
    - src/lib/i18n.ts contains 'errorInvalidEmail' inside 'auth' translations dictionary.
  </acceptance_criteria>
  <done>Error and loading translations registered.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Validation, Loading, and Redirection Logic in LoginScreen</name>
  <files>src/features/auth/components/LoginScreen.tsx</files>
  <read_first>src/features/auth/components/LoginScreen.tsx</read_first>
  <action>
    Modify src/features/auth/components/LoginScreen.tsx:
    - Add state hooks:
      - `email` (string)
      - `password` (string)
      - `errors` (object: { email?: string; password?: string })
      - `isLoading` (boolean)
    - Implement a `validateForm` helper:
      - Match email format using a standard email regex pattern. Set email error if invalid.
      - Check password length (`password.length >= 6`). Set password error if invalid.
      - Returns boolean (true if valid, false if invalid).
    - Refactor `handleSubmit`:
      - Call `validateForm()`. If invalid, prevent submission.
      - If valid, clear errors, set `isLoading` to true.
      - Trigger `setTimeout` delay of 1.5 seconds, then call `onLoginSuccess()`.
    - Update form fields in layout:
      - Bind `email` and `password` values and `onChange` handlers.
      - Show red error labels below input fields when error state is present.
      - Disable all inputs, language switcher button, and submit button when `isLoading` is true.
      - Render the Lucide icon `Loader2` rotating (`animate-spin size-4 mr-2`) in the submit button when `isLoading` is true along with `auth.loadingText`.
  </action>
  <verify>Run the compiler and check that there are no linter/build errors.</verify>
  <acceptance_criteria>
    - LoginScreen.tsx handles `email` and `password` change events.
    - Submit handler calls `setTimeout` on valid submission.
    - Form inputs are disabled during loading state.
  </acceptance_criteria>
  <done>Validation, loading state, and redirection timer successfully integrated.</done>
</task>

</tasks>
