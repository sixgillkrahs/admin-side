---
phase: 02-user-directory-rbac-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/rbac/types/index.ts
  - src/features/rbac/components/UserDirectory.tsx
  - src/features/rbac/components/RbacManagement.tsx
autonomous: true
requirements:
  - RBAC-01
user_setup: []

must_haves:
  truths:
    - "src/features/rbac/types/index.ts defines User, Role, and Permission types"
    - "src/features/rbac/components/UserDirectory.tsx renders a table with name, email, role, and status"
    - "src/features/rbac/components/RbacManagement.tsx mounts UserDirectory"
  artifacts:
    - "src/features/rbac/types/index.ts"
    - "src/features/rbac/components/UserDirectory.tsx"
    - "src/features/rbac/components/RbacManagement.tsx"
  key_links:
    - "src/features/rbac/components/RbacManagement.tsx -> src/features/rbac/components/UserDirectory.tsx"
---

<objective>
Define RBAC domain types and build the searchable User Directory Table component, integrating it into the RbacManagement master screen.

Purpose: Delivers RBAC-01 (D-01 and D-02) to display the user accounts list with filtering capabilities.
Output: Core TypeScript interfaces, UserDirectory table view, and coordinator wiring.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-directory-rbac-integration/02-CONTEXT.md
@.planning/phases/02-user-directory-rbac-integration/02-UI-SPEC.md
@.planning/phases/02-user-directory-rbac-integration/02-RESEARCH.md
@.planning/phases/02-user-directory-rbac-integration/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define RBAC Types and Interfaces</name>
  <files>
    src/features/rbac/types/index.ts
  </files>
  <read_first>
    .planning/phases/02-user-directory-rbac-integration/02-CONTEXT.md
  </read_first>
  <action>
    Create the types file `src/features/rbac/types/index.ts`:
    - Define types `Role` (e.g. 'Admin' | 'Moderator' | 'User').
    - Define type `Status` (e.g. 'active' | 'inactive').
    - Define interface `User` with fields: `id` (string), `name` (string), `email` (string), `role` (Role), `status` (Status), and `joinedDate` (string).
    - Define interface `PermissionClaim` mapping permission strings to active states.
  </action>
  <verify>
    Ensure type file compiles:
    `npm run build`
  </verify>
  <acceptance_criteria>
    - `src/features/rbac/types/index.ts` contains `export type Role`
    - `src/features/rbac/types/index.ts` contains `export interface User`
  </acceptance_criteria>
  <done>TypeScript type mappings are defined and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Build Searchable User Directory Table Component</name>
  <files>
    src/features/rbac/components/UserDirectory.tsx,
    src/features/rbac/components/RbacManagement.tsx
  </files>
  <read_first>
    src/features/rbac/types/index.ts,
    .planning/phases/02-user-directory-rbac-integration/02-UI-SPEC.md
  </read_first>
  <action>
    1. Implement `src/features/rbac/components/UserDirectory.tsx`:
       - Accepts `users` (array of `User`) and `onEditRole` (callback function when edit button is clicked) as props.
       - Implements a text input field to search by name or email.
       - Renders a clean table with columns: Name (with initial-based Avatar circle), Email, Role Badge (custom colors), Status Badge (green/gray dot), and Actions (containing a button to edit user's role).
       
    2. Overwrite `src/features/rbac/components/RbacManagement.tsx`:
       - Declares a reactive list of mock users (5 entries with active/inactive statuses and Admin/Moderator/User roles).
       - Imports and mounts `UserDirectory` passing the users list.
  </action>
  <verify>
    Ensure compile and lint checks pass:
    `npm run lint` and `npm run build`
  </verify>
  <acceptance_criteria>
    - `src/features/rbac/components/UserDirectory.tsx` filters users based on search input
    - `src/features/rbac/components/RbacManagement.tsx` mounts `<UserDirectory`
  </acceptance_criteria>
  <done>User directory table is rendered and mounted with local search filtering wired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build compiles with `npm run build`
- [ ] Lint checks pass with `npm run lint`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-directory-rbac-integration/02-01-SUMMARY.md`
</output>
