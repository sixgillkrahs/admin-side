---
phase: 02-user-directory-rbac-integration
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/features/rbac/components/PermissionMatrix.tsx
  - src/features/rbac/components/RbacManagement.tsx
autonomous: true
requirements:
  - RBAC-02
user_setup: []

must_haves:
  truths:
    - "src/features/rbac/components/PermissionMatrix.tsx renders permission rows and role column checkboxes"
    - "src/features/rbac/components/RbacManagement.tsx mounts PermissionMatrix"
  artifacts:
    - "src/features/rbac/components/PermissionMatrix.tsx"
  key_links:
    - "src/features/rbac/components/RbacManagement.tsx -> src/features/rbac/components/PermissionMatrix.tsx"
---

<objective>
Build the Grid-based Permission Matrix component showing mapped role claims, and integrate it into the RbacManagement master screen.

Purpose: Delivers RBAC-02 (D-03) to show the access rights structure by role.
Output: PermissionMatrix grid component and coordinator wiring.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-user-directory-rbac-integration/02-CONTEXT.md
@.planning/phases/02-user-directory-rbac-integration/02-UI-SPEC.md
@.planning/phases/02-user-directory-rbac-integration/02-RESEARCH.md
@.planning/phases/02-user-directory-rbac-integration/02-PATTERNS.md
@.planning/phases/02-user-directory-rbac-integration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Grid-based Permission Matrix Component</name>
  <files>
    src/features/rbac/components/PermissionMatrix.tsx,
    src/features/rbac/components/RbacManagement.tsx
  </files>
  <read_first>
    .planning/phases/02-user-directory-rbac-integration/02-CONTEXT.md,
    .planning/phases/02-user-directory-rbac-integration/02-UI-SPEC.md
  </read_first>
  <action>
    1. Implement `src/features/rbac/components/PermissionMatrix.tsx`:
       - Declares permissions list (e.g. read:messages, write:messages, admin:all, manage:roles).
       - Declares roles list (Admin, Moderator, User).
       - Renders a clean grid layout or HTML table where rows are permissions and columns are roles.
       - Renders checkboxes displaying mapped permission states (disabled since this is read-only matrix).
       
    2. Update `src/features/rbac/components/RbacManagement.tsx`:
       - Import and mount `<PermissionMatrix />` alongside `<UserDirectory />`.
       - Lay out the screen with section divisions matching premium typography styles.
  </action>
  <verify>
    Ensure type safety and compilation:
    `npm run build`
  </verify>
  <acceptance_criteria>
    - `src/features/rbac/components/PermissionMatrix.tsx` renders checkboxes for Admin, Moderator, and User claims
    - `src/features/rbac/components/RbacManagement.tsx` imports `PermissionMatrix`
  </acceptance_criteria>
  <done>Permission matrix is rendered and integrated as a secondary dashboard section.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build compiles with `npm run build`
- [ ] Lint checks pass with `npm run lint`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-directory-rbac-integration/02-02-SUMMARY.md`
</output>
